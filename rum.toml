[image]
base = "https://cloud-images.ubuntu.com/noble/current/noble-server-cloudimg-amd64.img"

[resources]
cpus = 4
memory_mb = 2048

[network]
nat = true                  # default: true — adds NAT NIC via "default" network
hostname = "myhost"         # default: VM name

[[network.interfaces]]
network = "rum-hostonly"    # libvirt network name
ip = "192.168.50.10"       # optional: DHCP reservation via stable MAC

[[network.interfaces]]
network = "dev-net"         # no ip = plain DHCP

[provision.system]
script = """
echo ===============================
echo initial system setup
echo ===============================
"""

[provision.boot]
script = """
echo ===============================
echo run on each boot
echo ===============================
echo "did-run-on-boot" > /did-run.log
"""

[[mounts]]
source = "."              # "." = rum.toml dir, "git" = repo root, or absolute path
target = "/mnt/project"
readonly = false           # default: false
tag = "project"            # default: auto-generated from target

[drives.data]
size = "10G"

[drives.logs1]
size = "50G"

[drives.logs2]
size = "50G"

[[fs.ext4]]
drive = "data"
target = "/mnt/data"

[[fs.zfs]]
drives = ["logs1", "logs2"]
target = "/mnt/logs"
pool = "logspool"              # optional: defaults to first drive name
